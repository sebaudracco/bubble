package com.vungle.publisher;

import com.vungle.publisher.cy.a;

/* compiled from: vungle */
public abstract class jj<T extends jk<T, P, E, ?, ?>, P extends jj<T, P, E>, E extends jl<P>> extends cy<T, P, E> {

    /* compiled from: vungle */
    protected static abstract class C4225a<T extends jk<T, P, E, A, V>, P extends jj<T, P, E>, E extends jl<P>, A extends jh<A, V, ?>, V extends jg<A>> extends a<T, P, E> {
        protected C4225a() {
        }
    }
}
